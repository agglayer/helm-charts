apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "agglayer.fullname" . }}-test-connection"
  labels:
    {{- include "agglayer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "agglayer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
