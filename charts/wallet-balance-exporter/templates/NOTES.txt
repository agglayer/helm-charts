Thank you for installing {{ .Chart.Name }}!

Your release is named {{ .Release.Name }}.

The wallet balance exporter Deployment has been deployed with the following configuration:

  Check Interval: Every {{ .Values.config.checkInterval.seconds }} seconds
  Secret: {{ .Values.existingSecret.name }}/{{ .Values.existingSecret.field }}
  Metrics Port: {{ .Values.ports.metrics }}
  Environment: {{ .Values.env }}

To verify the Deployment was created:

  kubectl get deployment {{ include "wallet-balance-exporter.fullname" . }} -n {{ .Release.Namespace }}

To view the pod status:

  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "wallet-balance-exporter.name" . }}

To view live logs:

  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "wallet-balance-exporter.name" . }} -f

To check metrics endpoint:

  kubectl port-forward -n {{ .Release.Namespace }} deployment/{{ include "wallet-balance-exporter.fullname" . }} 8080:{{ .Values.ports.metrics }}
  # Then visit http://localhost:8080/metrics

Note: Datadog Autodiscovery is always enabled. Make sure you have the Datadog agent
running in your cluster to collect metrics.

For more information and production considerations, see:
  https://github.com/agglayer/helm-charts/tree/main/charts/{{ .Chart.Name }}

Happy monitoring! 🔍

