apiVersion: v1
kind: Service
metadata:
  name: {{ include "aggkit-prover.fullname" . }}
  labels:
    {{- include "aggkit-prover.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    {{- include "aggkit-prover.selectorLabels" . | nindent 4 }}
