{{- if .Values.httpRoutes.aggsender.create -}}
apiVersion: gateway.networking.k8s.io/v1beta1
kind: HTTPRoute
metadata:
  name: {{ include "aggkit.fullname" . }}-aggsender
  labels:
    {{- include "aggkit.labels" . | nindent 4 }}
spec:
  parentRefs:
    - kind: Gateway
      {{- with .Values.httpRoutes.aggsender.gateway }}
      name: {{ .name }}
      namespace: {{ .namespace }}
      {{- end }}
  hostnames:
    - {{ .Values.httpRoutes.aggsender.hostname }}
  rules:
    - matches:
      - path:
          type: PathPrefix
          value: /
      backendRefs:
        - name: {{ include "aggkit.fullname" . }}
          port: {{ .Values.container.ports.aggsender }}
{{- end }}
{{- if .Values.httpRoutes.bridge.create }}
---
apiVersion: gateway.networking.k8s.io/v1beta1
kind: HTTPRoute
metadata:
  name: {{ include "aggkit.fullname" . }}-bridge
  labels:
    {{- include "aggkit.labels" . | nindent 4 }}
spec:
  parentRefs:
    - kind: Gateway
      {{- with .Values.httpRoutes.bridge.gateway }}
      name: {{ .name }}
      namespace: {{ .namespace }}
      {{- end }}
  hostnames:
    - {{ .Values.httpRoutes.bridge.hostname }}
  rules:
    - matches:
      - path:
          type: PathPrefix
          value: /
      backendRefs:
        - name: {{ include "aggkit.fullname" . }}
          port: {{ .Values.container.ports.bridge }}
{{- end }}
{{- if .Values.httpRoutes.aggsenderValidator.create }}
---
apiVersion: gateway.networking.k8s.io/v1beta1
kind: HTTPRoute
metadata:
  name: {{ include "aggkit.fullname" . }}-aggsender-validator
  labels:
    {{- include "aggkit.labels" . | nindent 4 }}
spec:
  parentRefs:
    - kind: Gateway
      {{- with .Values.httpRoutes.aggsenderValidator.gateway }}
      name: {{ .name }}
      namespace: {{ .namespace }}
      {{- end }}
  hostnames:
    - {{ .Values.httpRoutes.aggsenderValidator.hostname }}
  rules:
    - matches:
      - path:
          type: PathPrefix
          value: /
      backendRefs:
        - name: {{ include "aggkit.fullname" . }}
          port: {{ .Values.container.ports.validator }}
{{- end }}
---
apiVersion: networking.gke.io/v1
kind: HealthCheckPolicy
metadata:
  name: {{ include "aggkit.fullname" . }}-health
spec:
  default:
    checkIntervalSec: 30
    timeoutSec: 5
    healthyThreshold: 2
    unhealthyThreshold: 5
    config:
      type: HTTP
      httpHealthCheck:
        port: {{ .Values.container.ports.prometheus }}
        requestPath: /metrics
  targetRef:
    group: ""
    kind: Service
    name: {{ include "aggkit.fullname" . }}
