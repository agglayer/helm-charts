{{- if .Values.stats.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Release.Name }}-stats
  {{- with .Values.stats.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  tls:
    - hosts:
        - {{ .Values.stats.ingress.hostname }}
      secretName: "{{ .Values.stats.ingress.hostname }}-secret-tls"
  rules:
    - host: {{ .Values.stats.ingress.hostname }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Release.Name }}-stats
                port:
                  name: app
{{- end }}