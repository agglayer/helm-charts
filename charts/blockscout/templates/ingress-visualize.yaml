{{- if .Values.visualize.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Release.Name }}-visualize
  {{- with .Values.visualize.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  tls:
    - hosts:
        - {{ .Values.visualize.ingress.hostname }}
      secretName: "{{ .Values.visualize.ingress.hostname }}-secret-tls"
  rules:
    - host: {{ .Values.visualize.ingress.hostname }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Release.Name }}-visualize
                port:
                  name: app
{{- end }}