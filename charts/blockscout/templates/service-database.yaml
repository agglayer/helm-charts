apiVersion: v1
kind: Service
metadata:
  name: {{ include "blockscout.componentName" (dict "componentNameKey" "postgres" "Values" .Values "Release" .Release "Chart" .Chart) }}-service
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "blockscout.labels" . | nindent 4 }}
spec:
  selector:
    app.kubernetes.io/name: {{ include "blockscout.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    {{- include "blockscout.selectorLabels" . | nindent 4 }}
