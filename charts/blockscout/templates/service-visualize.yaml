apiVersion: v1
kind: Service
metadata:
  name: {{ include "blockscout.componentName" (dict "componentNameKey" "visualize" "Values" .Values "Release" .Release "Chart" .Chart) }}-service
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "blockscout.labels" . | nindent 4 }}
spec:
  selector:
    app.kubernetes.io/name: {{ include "blockscout.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - port: {{ .Values.blockscout.visualize.port }}
      targetPort: {{ .Values.blockscout.visualize.port }}
  selector:
    {{- include "blockscout.selectorLabels" . | nindent 4 }}
