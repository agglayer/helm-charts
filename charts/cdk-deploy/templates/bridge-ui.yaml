{{- if .Values.bridgeUi.enabled -}}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.bridgeUi.releaseName }}
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "500"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: {{ .Values.chainName }}
  sources:
  - repoURL: europe-west2-docker.pkg.dev/prj-polygonlabs-shared-prod/polygonlabs-docker-prod
    chart: bridge-ui
    targetRevision: {{ .Values.bridgeUi.targetRevision }}
    helm:
      releaseName: {{ .Values.bridgeUi.releaseName }}
      valueFiles:
        {{- range $.Values.bridgeUi.values.files }}
        - $values/{{ . }}
        {{- end }}    
  - repoURL: {{ .Values.bridgeUi.values.repoUrl }}
    targetRevision: {{ .Values.bridgeUi.values.targetRevision }}
    ref: values
  destination:
    server: "https://kubernetes.default.svc"
    namespace: {{ .Values.namespace }}
  syncPolicy:
    automated:
      prune: true
{{- end }}
