{{- if .Values.explorer.enabled -}}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.chainName }}-{{ .Values.explorer.releaseName }}
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "500"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: {{ .Values.chainName }}
  sources:
  - repoURL: europe-west2-docker.pkg.dev/prj-polygonlabs-shared-prod/polygonlabs-docker-prod
    chart: zkevm-explorer
    targetRevision: {{ .Values.explorer.targetRevision }}
    helm:
      releaseName: {{ .Values.explorer.releaseName }}
      valueFiles:
        {{- range $.Values.explorer.values.files }}
        - $values/{{ . }}
        {{- end }}    
  - repoURL: {{ .Values.explorer.values.repoUrl }}
    targetRevision: {{ .Values.explorer.values.targetRevision }}
    ref: values
  destination:
    server: "https://kubernetes.default.svc"
    namespace: {{ .Values.namespace }}
  syncPolicy:
    automated:
      prune: true
{{- end }}
