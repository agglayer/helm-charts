{{- if .Values.cdkNode.enabled -}}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.cdkNode.releaseName }}
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "400"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: {{ .Values.chainName }}
  sources:
  - repoURL: europe-west2-docker.pkg.dev/prj-polygonlabs-shared-prod/polygonlabs-docker-prod
    chart: cdk
    targetRevision: {{ .Values.cdkNode.targetRevision }}
    helm:
      releaseName: {{ .Values.cdkNode.releaseName }}
      valueFiles:
        {{- range $.Values.cdkNode.values.files }}
        - $values/{{ . }}
        {{- end }}    
  - repoURL: {{ .Values.cdkNode.values.repoUrl }}
    targetRevision: {{ .Values.cdkNode.values.targetRevision }}
    ref: values
  destination:
    server: "https://kubernetes.default.svc"
    namespace: {{ .Values.namespace }}
  syncPolicy:
    automated:
      prune: true
{{- end }}
