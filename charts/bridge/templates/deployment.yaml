apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "bridge.fullname" . }}
  labels:
    {{- include "bridge.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "bridge.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "bridge.labels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "bridge.serviceAccountName" . }}
      securityContext:
        fsGroup: 1001
        fsGroupChangePolicy: Always
        seccompProfile:
          type: RuntimeDefault
      {{- with .Values.tolerations }}
      tolerations:
        {{- . | toYaml | nindent 8 }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- . | toYaml | nindent 8 }}
      {{- end }}
      {{- if or .Values.envSubstitution.enabled .Values.initContainers }}
      initContainers:
        {{- if .Values.envSubstitution.enabled }}
        - name: config-env-substitution
          image: alpine:latest
          command: ["/bin/sh"]
          args:
            - "-c"
            - |
              apk add gettext > /dev/null 2>&1
              envsubst < /config-raw/bridge-config.toml > /config/bridge-config.toml
          envFrom:
            {{- range $name, $_ := $.Values.onePasswordSecrets }}
            - secretRef:
                name: {{ $name }}
            {{- end }}
          volumeMounts:
            - name: config-raw
              mountPath: /config-raw
              readOnly: true
            - name: config
              mountPath: /config
        {{- end }}
        {{- with .Values.initContainers }}
        {{- . | toYaml | nindent 8 }}
        {{- end }}
      {{- end }}
      {{- with .Values.topologySpreadConstraints }}
      topologySpreadConstraints:
        {{- range $constraint := . }}
        - {{ toYaml $constraint | nindent 10 | trim }}
            {{- if not $constraint.labelSelector }}
          labelSelector:
            matchLabels:
              {{- include "bridge.selectorLabels" $ | nindent 14 }}
            {{- end }}
        {{- end }}
      {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
              - ALL
            privileged: false
            readOnlyRootFilesystem: false
            runAsGroup: 0
            runAsNonRoot: true
            runAsUser: 1001
            seccompProfile:
              type: RuntimeDefault
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          {{- if .Values.image.command }}
          command: {{- range .Values.image.command }}
            - {{ . }} {{- end}}
          {{- end }}
          {{- if .Values.image.args }}
          args: {{- range .Values.image.args }}
            - {{ . }} {{- end}}
          {{- end }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          {{- with .Values.livenessProbe }}
          livenessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.readinessProbe }}
          readinessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - name: config
              mountPath: /etc/bridge/
              readOnly: true
            {{- if eq .Values.config.claimTxManager.privateKey.keyName "" }}
            - name: keystores
              mountPath: /etc/bridge/keystores/
              readOnly: true
            {{- end }}
            {{- with .Values.extraVolumeMounts }}
            {{- . | toYaml | nindent 12 }}
            {{- end }}
      volumes:
        - name: {{ ternary "config-raw" "config" .Values.envSubstitution.enabled }}
          configMap:
            name: {{ include "bridge.fullname" . }}
            items:
              - key: "bridge-config.toml"
                path: "bridge-config.toml"
        {{- if .Values.envSubstitution.enabled }}
        - name: config
          emptyDir: {}
        {{- end }}
        {{- if eq .Values.config.claimTxManager.privateKey.keyName "" }}
        - name: keystores
          secret:
            secretName: {{ include "bridge.fullname" . }}
            items:
              - key: {{ .Values.config.claimTxManager.privateKey.keystoreFileName }}
                path: {{ .Values.config.claimTxManager.privateKey.keystoreFileName }}
        {{- end }}
        {{- with .Values.extraVolumes }}
        {{- . | toYaml | nindent 8 }}
        {{- end }}
