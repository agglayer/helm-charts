apiVersion: v1
kind: Secret
metadata:
  name: {{ include "agg-certificate-proxy.fullname" . }}-cloudflare-origin-cert
  namespace: {{ .Release.Namespace }}
type: kubernetes.io/tls
data:
  tls.crt: {{ .Values.tls.base64Cert }}
  tls.key: {{ .Values.tls.base64Key }}
